import 'package:flutter/material.dart';

class Update_Tab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Updates')),
    );
  }
}
